import { GraphQLResolveInfo, GraphQLSchema } from "graphql";
import { Driver } from "neo4j-driver";
import { Neo4jGraphQLConfig, Node, Relationship } from "../../classes";
import { Context, Neo4jGraphQLPlugins } from "../../types";
export declare const wrapResolver: ({ driver, config, nodes, relationships, schema, plugins, }: {
    driver?: Driver | undefined;
    config: Neo4jGraphQLConfig;
    nodes: Node[];
    relationships: Relationship[];
    schema: GraphQLSchema;
    plugins?: Neo4jGraphQLPlugins | undefined;
}) => (next: any) => (root: any, args: any, context: Context, info: GraphQLResolveInfo) => Promise<any>;
//# sourceMappingURL=wrapper.d.ts.map