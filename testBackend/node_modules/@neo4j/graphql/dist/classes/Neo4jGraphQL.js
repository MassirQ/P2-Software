"use strict";
/*
 * Copyright (c) "Neo4j"
 * Neo4j Sweden AB [http://neo4j.com]
 *
 * This file is part of Neo4j.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const schema_1 = require("@graphql-tools/schema");
const resolvers_composition_1 = require("@graphql-tools/resolvers-composition");
const utils_1 = require("@graphql-tools/utils");
const merge_1 = require("@graphql-tools/merge");
const schema_2 = require("../schema");
const verify_database_1 = __importDefault(require("./utils/verify-database"));
const asserts_indexes_and_constraints_1 = __importDefault(require("./utils/asserts-indexes-and-constraints"));
const wrapper_1 = require("../schema/resolvers/wrapper");
const resolvers_1 = require("../schema/resolvers");
const utils_2 = require("../utils/utils");
class Neo4jGraphQL {
    constructor(input) {
        const { config = {}, driver, plugins, ...schemaDefinition } = input;
        this.driver = driver;
        this.config = config;
        this.plugins = plugins;
        this.schemaDefinition = schemaDefinition;
    }
    get nodes() {
        if (!this._nodes) {
            throw new Error("You must await `.getSchema()` before accessing `nodes`");
        }
        return this._nodes;
    }
    get relationships() {
        if (!this._relationships) {
            throw new Error("You must await `.getSchema()` before accessing `relationships`");
        }
        return this._relationships;
    }
    async getSchema() {
        if (!this.schema) {
            this.schema = this.generateSchema();
        }
        return this.schema;
    }
    async checkNeo4jCompat(input = {}) {
        var _a;
        const driver = input.driver || this.driver;
        const driverConfig = input.driverConfig || ((_a = this.config) === null || _a === void 0 ? void 0 : _a.driverConfig);
        if (!driver) {
            throw new Error("neo4j-driver Driver missing");
        }
        return (0, verify_database_1.default)({ driver, driverConfig });
    }
    async assertIndexesAndConstraints(input = {}) {
        var _a;
        if (!this.schema) {
            throw new Error("You must call `.getSchema()` before `.assertIndexesAndConstraints()`");
        }
        await this.schema;
        const driver = input.driver || this.driver;
        const driverConfig = input.driverConfig || ((_a = this.config) === null || _a === void 0 ? void 0 : _a.driverConfig);
        if (!driver) {
            throw new Error("neo4j-driver Driver missing");
        }
        await (0, asserts_indexes_and_constraints_1.default)({ driver, driverConfig, nodes: this.nodes, options: input.options });
    }
    addDefaultFieldResolvers(schema) {
        (0, utils_1.forEachField)(schema, (field) => {
            if (!field.resolve) {
                // eslint-disable-next-line no-param-reassign
                field.resolve = resolvers_1.defaultFieldResolver;
            }
        });
        return schema;
    }
    wrapResolvers(resolvers, { schema }) {
        const wrapResolverArgs = {
            driver: this.driver,
            config: this.config,
            nodes: this.nodes,
            relationships: this.relationships,
            schema,
            plugins: this.plugins,
        };
        const resolversComposition = {
            "Query.*": [(0, wrapper_1.wrapResolver)(wrapResolverArgs)],
            "Mutation.*": [(0, wrapper_1.wrapResolver)(wrapResolverArgs)],
        };
        // Merge generated and custom resolvers
        const mergedResolvers = (0, merge_1.mergeResolvers)([resolvers, ...(0, utils_2.asArray)(this.schemaDefinition.resolvers)]);
        return (0, resolvers_composition_1.composeResolvers)(mergedResolvers, resolversComposition);
    }
    addWrappedResolversToSchema(resolverlessSchema, resolvers) {
        const schema = (0, schema_1.addResolversToSchema)(resolverlessSchema, resolvers);
        return this.addDefaultFieldResolvers(schema);
    }
    generateSchema() {
        return new Promise((resolve) => {
            var _a, _b;
            const { nodes, relationships, typeDefs, resolvers } = (0, schema_2.makeAugmentedSchema)(this.schemaDefinition.typeDefs, {
                enableRegex: (_a = this.config) === null || _a === void 0 ? void 0 : _a.enableRegex,
                skipValidateTypeDefs: (_b = this.config) === null || _b === void 0 ? void 0 : _b.skipValidateTypeDefs,
            });
            this._nodes = nodes;
            this._relationships = relationships;
            const resolverlessSchema = (0, schema_1.makeExecutableSchema)({
                ...this.schemaDefinition,
                typeDefs,
            });
            // Wrap the generated resolvers, which adds a context including the schema to every request
            const wrappedResolvers = this.wrapResolvers(resolvers, { schema: resolverlessSchema });
            const schema = this.addWrappedResolversToSchema(resolverlessSchema, wrappedResolvers);
            resolve(schema);
        });
    }
}
exports.default = Neo4jGraphQL;
//# sourceMappingURL=Neo4jGraphQL.js.map