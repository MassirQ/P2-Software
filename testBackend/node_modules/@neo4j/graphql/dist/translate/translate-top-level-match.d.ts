import { Node } from "../classes";
import { AuthOperations, Context } from "../types";
declare function translateTopLevelMatch({ node, context, varName, operation, }: {
    context: Context;
    node: Node;
    varName: string;
    operation: AuthOperations;
}): [string, Record<string, unknown>];
export default translateTopLevelMatch;
//# sourceMappingURL=translate-top-level-match.d.ts.map